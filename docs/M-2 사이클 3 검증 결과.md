# M-2 사이클 3 검증 결과 (M-2 종료)

**날짜:** 2026-05-03
**범위:** 신규 `test_multi_map_transition.gd` 시뮬 검증

## 변경

### 신규
- `test/test_multi_map_transition.gd` + `.tscn` (21 항목):
  - ruin_01 정합 / 양방향 transition 슬롯 검증
  - field (13,6) → (14,6) 진입 → ruin (0,5) 도착 검증
  - 도착지 슬롯 자동 평가 X (transition 자동 발화 X) 검증
  - ruin 안 이동 + ruin_dust 발화 검증
  - 복귀 (ruin (0,5) → field (14,6))
  - 회귀 시 시작 맵 자동 복귀

### 버그 수정
- `MAPS["ruin_01"]["encounters"][Vector2i(0,5)]["on_enter"]["target_pos"]` 가 `Vector2i(15, 6)` 으로 박혀있었음 (외곽 벽 위치). `Vector2i(14, 6)` 으로 수정.

## 검증

### 회귀 + 신규
| 테스트 | 결과 |
|---|---|
| test_event_1a / 1a2 / 1b / 2 | 102 / 102 |
| test_grid | 49 / 49 |
| test_save_load | 36 / 36 |
| test_multi_map_schema | 24 / 24 |
| **test_multi_map_transition (신규)** | **21 / 21** |
| **합** | **232 / 232 PASS** |

### 외부 동작
field (14, 6) 진입 시 ruin (0, 5) 으로 전이. 그 외 동작 그대로.

## M-2 종료

3 사이클 / 10 unit 완료. 다중 맵 + transition 작동. ruin_01 안 이동 / event / combat 슬롯 정상.

## 다음

**M-3** — UI 다중 그리드. `_grid_roots` dict + 모든 맵 미리 빌드 + `current_map_id` 결로 가시성 토글.
