# M-1 사이클 2 검증 결과

**날짜:** 2026-05-03
**범위:** `run_data` 의 신규 필드 (`current_map_id` / `visited_by_map` / `explored_by_map`)

## 변경

`run_manager.gd::_start_internal_run`:
- 신설 `run_data["current_map_id"] = STARTING_MAP`
- 신설 `run_data["visited_by_map"] = { STARTING_MAP: { spawn: true } }`
- 신설 `run_data["explored_by_map"] = { STARTING_MAP: {} }`
- `player_pos` / `day` / `actions_*` / `seen_this_run` / `pending_combat` / `phase` 그대로
- 옛 `visited_tiles` / `explored_tiles` 임시 병행 — 참조처 갱신 동안. 사이클 5 폐기.

## 결정 사항

방법론 결: 사이클 2 만으로 옛 키 폐기 시 참조처 (try_move / try_explore / 테스트) 다수 깨짐. 두 결 (옛 + 새) 병행 결로 점진 마이그레이션. 사이클 3 (조회자) → 사이클 4 (액션) 이 새 결 사용 → 사이클 5 폐기.

## 검증

### 회귀
| 테스트 | 결과 |
|---|---|
| test_event_1a | 27 / 27 |
| test_event_1a2 | 28 / 28 |
| test_event_1b | 19 / 19 |
| test_event_2 | 28 / 28 |
| test_grid | 48 / 48 |
| test_save_load | 36 / 36 |
| **합** | **186 / 186 PASS** |

### 외부 동작 변경
0 — 옛 필드 그대로 + 신규 필드 박힘.

## 다음

사이클 3 — 조회자 함수 (`_current_map` / `_get_terrain_at` / `get_tile_encounter`) 가 `current_map_id` 기반.
