# M-1 사이클 4 검증 결과

**날짜:** 2026-05-03
**범위:** 액션 함수 — `try_move` / `try_explore` 가 `visited_by_map` / `explored_by_map` 결로 갱신

## 변경

`run_manager.gd`:
- `try_move` — 이동 후 `visited_by_map[current_map_id][target] = true` 박음. 옛 `visited_tiles` 도 호환 결로 병행 (사이클 5 폐기).
- `try_explore` — `explored_by_map[current_map_id]` 결로 갱신. 옛 `explored_tiles` 병행.
- 두 함수 모두 새 결의 dict 자동 신설 (해당 map_id 의 키 없으면 빈 dict 박음).
- `_check_on_enter` / `_check_on_explore` 변경 없음 (조회자만 사이클 3 에서 갱신됨).

## 결과

매니저 단의 모든 액션 / 조회자가 `current_map_id` 기반. 옛 `visited_tiles` / `explored_tiles` 의 사용처는 매니저 단 0 (병행 박기만 남음 — 사이클 5 폐기 대상). UI / 테스트 의 옛 키 참조는 사이클 5 정리.

## 검증

### 회귀
| 테스트 | 결과 |
|---|---|
| test_event_1a | 27 / 27 |
| test_event_1a2 | 28 / 28 |
| test_event_1b | 19 / 19 |
| test_event_2 | 28 / 28 |
| test_grid | 48 / 48 |
| test_save_load | 36 / 36 |
| **합** | **186 / 186 PASS** |

### 외부 동작 변경
0 — 새 결과 옛 결 둘 다 동시 박혀 있어 모든 참조처 자동 호환.

## 다음

사이클 5 — UI 호환 (`run_ui.gd` 의 `WORLD_TERRAIN` / `TILE_ENCOUNTERS` / `SPAWN_POS` 직접 참조 정리) + 신규 검증 `test_multi_map_schema.gd` + 옛 const / 옛 키 폐기.
