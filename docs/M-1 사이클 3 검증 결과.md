# M-1 사이클 3 검증 결과

**날짜:** 2026-05-03
**범위:** 조회자 함수 — `_current_map` / `_get_terrain_at` / `get_tile_encounter`

## 변경

`run_manager.gd`:
- 신설 `_current_map()` — `run_data["current_map_id"]` 결로 `MAPS[map_id]` 반환.
- 갱신 `_get_terrain_at(pos)` — `_current_map()["terrain"]` 결로 분기 (옛: `GameData.WORLD_TERRAIN` 직접 참조).
- 갱신 `get_tile_encounter(pos)` — `_current_map()["encounters"]` 결로 분기 (옛: `GameData.TILE_ENCOUNTERS`).
- 신설 `get_current_map_name()` — UI HUD 결.
- `_is_passable` / `get_terrain_name` 변경 없음 (`_get_terrain_at` 위에 얹힘 — 자동 호환).

## 결과

옛 const (`WORLD_TERRAIN` / `TILE_ENCOUNTERS`) 의 매니저 단 직접 참조 0. 단 legacy alias 결로 `game_data.gd` 에 const 자체는 살아있음 (사이클 5 폐기). UI / 테스트 의 참조는 사이클 4~5 갱신.

## 검증

### 회귀
| 테스트 | 결과 |
|---|---|
| test_event_1a | 27 / 27 |
| test_event_1a2 | 28 / 28 |
| test_event_1b | 19 / 19 |
| test_event_2 | 28 / 28 |
| test_grid | 48 / 48 |
| test_save_load | 36 / 36 |
| **합** | **186 / 186 PASS** |

### 외부 동작 변경
0 — `_current_map()` 가 `STARTING_MAP` (= `field_01`) 결의 동일 데이터 반환.

## 다음

사이클 4 — 액션 함수 (`try_move` / `try_explore`) 가 `visited_by_map` / `explored_by_map` 결로 갱신. 옛 `visited_tiles` / `explored_tiles` 키 사용처 정리.
