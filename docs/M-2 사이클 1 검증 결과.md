# M-2 사이클 1 검증 결과

**날짜:** 2026-05-03
**범위:** `MAPS["ruin_01"]` 정의 + 라이브러리 이벤트 2 종

## 변경

### `game_data.gd`
- `MAPS["ruin_01"]` 신설:
  - 10×8 그리드 (작은 결로 시작)
  - `name`: "폐허"
  - `spawn`: (0, 5)
  - 4 슬롯:
    - (0, 5) `ruin_to_field` — transition (field_01 (15, 6) 으로)
    - (4, 4) `ruin_dust` — event 단편
    - (7, 3) `ruin_combat_big_worm` — combat (big_worm)
    - (2, 6) `ruin_buried_chest` — explore event (once_per: big_run)
- `EVENTS` 에 `ruin_dust` / `ruin_buried_chest` 라이브러리 dialogue 추가

## 검증

### 회귀
| 테스트 | 결과 |
|---|---|
| test_event_1a / 1a2 / 1b / 2 | 102 / 102 |
| test_grid | 49 / 49 |
| test_save_load | 36 / 36 |
| test_multi_map_schema | 24 / 24 |
| **합** | **211 / 211 PASS** |

### 외부 동작 변경
0 — `field_01` 안 동작 그대로. `ruin_01` 은 정의만, 진입 결로 아직 도달 X (transition 슬롯이 미구현).

## 다음

사이클 2 — `field_01` 의 (15, 6) 에 transition 슬롯 + `_check_on_enter` 의 transition 분기 구현.
