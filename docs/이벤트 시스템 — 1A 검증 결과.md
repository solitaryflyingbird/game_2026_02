# 이벤트 시스템 — Stage 1A 검증 결과

**날짜:** 2026-04-30  **결과:** ✅ PASS (25 / 25)

선행: `이벤트 시스템 구현 — 안 4 (kind 분리 + chain).md`, `이벤트 시스템 — 안 4 구현 롤아웃 (단계별 공유·검증).md`, `범용적 원칙/헤드리스 자동 검증.md`

---

## §1. 산출물

### 신규 파일
- `event_manager.gd` — 오토로드. `event_state` 일시 상태, `begin_event` / `advance_line` / `_finalize_event` / `_resolve_event_for_node`. dialogue kind 만 구현, 나머지 kind 는 스텁.
- `test/test_event_1a.gd` — 헤드리스 검증 시나리오 (25 체크).
- `test/test_event_1a.tscn` — 진입 씬.

### 변경 파일
- `project.godot` — `EventManager` 오토로드 등록.
- `game_data.gd` — `EVENTS` 상수 (intro_monologue / dialogue kind / once_per: big_run). `TEST_MAP_GRAPH` 노드 5 를 `type: "event"` 로 (기존 enemy_id "soldier" 제거).
- `run_manager.gd` — `_new_big_run` 에 `seen_events: {}`. `_ready` 에서 `EventManager.event_resolved` 구독. `_on_event_resolved` 추가 (seen_events 갱신 + phase 복귀). `move_to_node` 에 phase=="event" 가드 + type "event" 분기.

---

## §2. 헤드리스 실행 명령

```
"/Users/js/Desktop/Godot.app/Contents/MacOS/Godot" \
  --headless \
  --path "/Users/js/Desktop/퍼즐던전 고도" \
  res://test/test_event_1a.tscn
```

종료 코드 `0` = PASS, 그 외 = FAIL.

---

## §3. 검증 시나리오 (25 체크)

### 1. 초기 상태 (2)
- [x] 초기 `phase == "map"`
- [x] `seen_events == {}`

### 2. event 노드 진입 (7)
- [x] `move_to_node(5)` 성공
- [x] `phase == "event"` 전이
- [x] `event_state` 활성
- [x] `event_state.kind == "dialogue"`
- [x] `event_state.event_id == "intro_monologue"`
- [x] `event_state.line_idx == 0`
- [x] `event_state.chain_root_id == event_id`

### 3. 이벤트 중 외부 move 거부 (2)
- [x] 이벤트 활성 중 `move_to_node` → false 반환
- [x] 거부 후 `phase` 유지

### 4. 라인 진행 (2)
- [x] `advance_line` 1회 → `line_idx == 1`
- [x] advance 후에도 phase 유지

### 5. 이벤트 종료 (3)
- [x] 마지막 라인 advance → `event_state = {}`
- [x] phase = `"map"` 자동 복귀
- [x] `seen_events["intro_monologue"] == 1`

### 6. once_per "big_run" — 같은 큰 런 재진입 차단 (4)
- [x] 종료 후 일반 move 재개
- [x] event 노드 재진입 move 성공
- [x] phase = `"map"` 유지 (이벤트 미발생)
- [x] `event_state` 비활성 유지

### 7. 회귀 후 once_per 유지 (5)
- [x] `end_internal_run("cleared")` 후 `seen_events` 유지
- [x] 회귀 후 phase = `"map"`
- [x] 회귀 후 `current_node_id == 1`
- [x] 회귀 후 event 노드 이동 성공
- [x] 회귀 후에도 once_per 회피 — 이벤트 미발생

---

## §4. 콘솔 출력 (전체)

```
Godot Engine v4.6.1.stable.official.14d19694e

=== Stage 1A 자동 검증 시작 ===
[PASS] 초기 phase = 'map'
[PASS] seen_events 초기화 = {}
[PASS] event 노드 move 성공
[PASS] phase = 'event' 전이
[PASS] event_state 활성
[PASS] event_state.kind = 'dialogue'
[PASS] event_state.event_id = 'intro_monologue'
[PASS] event_state.line_idx = 0
[PASS] event_state.chain_root_id = event_id
WARNING: move_to_node: 이벤트 중 이동 불가 (phase = 'event')
[PASS] 이벤트 중 move_to_node 거부
[PASS] 거부 후 phase 유지 = 'event'
[PASS] advance 1회 → line_idx = 1
[PASS] advance 1회 후에도 phase = 'event'
[PASS] 종료 후 event_state = {}
[PASS] 종료 후 phase = 'map'
[PASS] seen_events[intro_monologue] = 1
[PASS] 이벤트 종료 후 일반 move 가능
[PASS] event 노드 재진입 move 성공
[PASS] once_per: 재진입 시 phase 'map' 유지 (이벤트 미발생)
[PASS] once_per: event_state 비활성 유지
[PASS] 회귀 후 seen_events 유지
[PASS] 회귀 후 phase = 'map'
[PASS] 회귀 후 current_node_id = 1
[PASS] 회귀 후 event 노드 이동 성공
[PASS] 회귀 후에도 once_per 회피 — 이벤트 미발생
---
총 25 검증 — PASS 25 / FAIL 0
=== PASS ===
EXIT=0
```

(WARNING 한 건은 § 3 의 "이벤트 중 move 거부" 검증 시 의도적으로 트리거한 push_warning. 정상.)

---

## §5. 자가점검 — 안 4 §0 (A~H) / 헤드리스 자동 검증 원칙

| 항목 | 상태 |
|---|---|
| **A.** EventManager 오토로드, event_state 비활성 시 {} | ✓ |
| **B.** kind 디스크리미네이터, dialogue 만 구현 | ✓ |
| **C.** dialogue 외 kind 스텁 (push_warning + 즉시 종료) | ✓ |
| **D.** chain (`next` 필드) 미구현 — 다음 차로 | (보류) |
| **E.** seen_events ∈ big_run_data, RunManager 단일 추적점 | ✓ |
| **F.** Type 1 = `RunManager._apply_*` 단일 경로 (1A 시점 dialogue 만이라 미발동) | ✓ |
| **G.** `event_resolved(result)` 한 번 emit, RunManager 가 수신 | ✓ |
| **H.** on/off 배타 — 이벤트 중 외부 move 거부 | ✓ |
| 헤드리스 자동 검증 — public API 만 호출 | ✓ |
| 명시적 assertion (assert 매크로 X, push_error + return false) | ✓ |
| 회귀 누적 — 테스트 씬 영구 보존 | ✓ |

---

## §6. 다음 단계

**Stage 1B — `event_ui.gd` + `main.tscn` 배선.**

작업 범위·검증 시나리오는 롤아웃 §3 참조. 변경 사항 (1A 와 비교):
- 신규: `event_ui.gd`, `main.tscn` 의 `event_screen` 노드.
- 변경: `run_ui.gd::screens` 에 `"event": $event_screen` 추가.
- 검증: 1A 의 25 체크 그대로 + UI 시각 검증 (스크린샷 캡처) 추가.

진입 GO 신호 주시면 1B 진입.
