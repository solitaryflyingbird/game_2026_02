# M-1 사이클 5 검증 결과 (M-1 종료)

**날짜:** 2026-05-03
**범위:** UI 호환 + 옛 const / 옛 키 폐기 + 신규 검증 `test_multi_map_schema`

## 변경

### `run_ui.gd`
- `_build_grid_display` — `GameData.WORLD_TERRAIN` / `TILE_ENCOUNTERS` 직접 참조 제거 → `MAPS[STARTING_MAP]["terrain"]` / `["encounters"]` 결로.
- `_refresh_grid_display` — `current_map_id` 결로 `visited_by_map[map_id]` + 해당 맵의 terrain 사용.
- `_refresh_grid_hud` — `current_map_id` 의 spawn / map name. HUD 에 맵 이름 prefix ("평원 — 풀밭 (1, 6)").

### `run_manager.gd`
- `_start_internal_run` 의 옛 `visited_tiles` / `explored_tiles` 박기 제거.
- `try_move` / `try_explore` 의 옛 키 병행 박기 제거.

### `game_data.gd`
- legacy alias 폐기 — `SPAWN_POS` / `WORLD_TERRAIN` / `TILE_ENCOUNTERS` 모두 제거.

### `test/`
- `test_event_1a` / `test_event_1a2` / `test_grid` / `test_save_load` — `GameData.SPAWN_POS` / `WORLD_TERRAIN` / `TILE_ENCOUNTERS` / `visited_tiles` / `explored_tiles` 모든 참조를 새 결로 갱신.

### 신규
- `test/test_multi_map_schema.gd` + `.tscn` — 24 검증 항목 (MAPS 정합 / STARTING_MAP / run_data 신규 필드 / 옛 결 폐기 검증 / 회귀 후 자동 리셋).

## 검증

### 회귀 + 신규
| 테스트 | 결과 |
|---|---|
| test_event_1a | 27 / 27 |
| test_event_1a2 | 28 / 28 |
| test_event_1b | 19 / 19 |
| test_event_2 | 28 / 28 |
| test_grid | 49 / 49 |
| test_save_load | 36 / 36 |
| **test_multi_map_schema (신규)** | **24 / 24** |
| **합** | **211 / 211 PASS** |

### 외부 동작 변경
0 — 1 맵 결의 모든 행동 그대로. 자료구조만 다중 맵 결로 wrapping.

### 옛 const / 키 폐기 검증
- `GameData.SPAWN_POS` / `WORLD_TERRAIN` / `TILE_ENCOUNTERS` — 코드베이스 어디에도 미참조 (헤드리스 검증 항목 14, 15, 16 PASS)
- `run_data["visited_tiles"]` / `["explored_tiles"]` — 어디에도 미참조 (검증 항목 22, 23 PASS)

## M-1 종료

5 사이클 22 unit 모두 완료. 외부 동작 변경 0. 신규 검증 24 + 기존 회귀 187 = 211 PASS.

다음: **M-2** — 두번째 맵 (`ruin_01`) + transition 슬롯 양방향.
