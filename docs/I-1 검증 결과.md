# I-1 검증 결과 — ITEMS 카탈로그 + 빈 dict 결

**날짜:** 2026-05-04
**범위:** 인벤토리 자료구조 신설 (RPG 결로 빈 dict 결 시작)

## 변경

### `game_data.gd`
- `CATEGORY_NAMES` const 신설 (consumable / material / quest 한국어 매핑)
- `ITEMS` const 신설 (5 종 placeholder):
  - food / repair_kit / rare_part / scanner / key
- 응급수리키트 (`repair_kit`) — 직전 결 (potion / 회복 약초) 정정 결

### `run_manager.gd`
- `_new_big_run` 의 `big_run_data["inventory"] = {}` (빈 dict 결)
- `_start_internal_run` 의 시점 복귀 결:
  - `run_data["inventory"] = big.inventory.duplicate()`
  - `run_data["tools"] = {}`

## 검증

### 신규 — `test_inventory_schema.gd`: 15 / 15 PASS
- ITEMS 카탈로그 (7) — 5 종 / scope / use_event_id / CATEGORY_NAMES
- 빈 dict 결 시작 (3) — big / run / tools
- 시점 복귀 (3) — run["inventory"] 직접 변경 후 회귀 → big 의 빈 결로 복원
- 회차 한정 (2) — tools 빈 dict, 회귀 시 빔, big 결 변경 X

### 회귀
| 테스트 | 결과 |
|---|---|
| 기존 9 결 (event/grid/save_load/multi_map_4) | 251 / 251 |
| **신규 inventory_schema** | **15 / 15** |
| **누적** | **266 / 266 PASS** |

### 외부 동작 변경
0 — 자료구조 신설만, 사용처 X.

## 다음

I-2 — helper 함수 (`_inventory_for_scope` / `add_item` / `_consume_item` / `use_item`). count 0 도달 시 erase 결 포함.
