# M-2 사이클 2 검증 결과

**날짜:** 2026-05-03
**범위:** transition 슬롯 양방향 + `_check_on_enter` 의 transition 분기

## 변경

### `game_data.gd`
- `MAPS["field_01"]["encounters"][Vector2i(14, 6)]` 신설:
  - `kind: "transition"`, `target_map: "ruin_01"`, `target_pos: Vector2i(0, 5)`
  - (15, 6) 은 외곽 벽 (F) 이라 (14, 6) 채택

### `run_manager.gd`
- `_check_on_enter` 의 match 에 `"transition"` 분기 추가
- 신설 `_do_transition(slot)`:
  - `seen_this_run[slot.id]` 기록
  - `current_map_id` / `player_pos` 갱신
  - 도착지 `visited_by_map[target_map]` / `explored_by_map[target_map]` 자동 신설
  - 도착 칸 visited 박힘
  - **도착지 슬롯 자동 평가 X** — 양 끝 transition 끼리 무한 루프 회피

### `test/test_multi_map_schema.gd`
- "encounters 5 슬롯" → "encounters >= 5 슬롯" (transition 추가로 6 됨)

## 결정 사항 — 무한 루프 회피

원 spec 은 "도착지 슬롯 자동 평가" 였음. 단 ruin_01 의 (0, 5) 에 ruin_to_field transition 슬롯이 있어, field 의 (14, 6) → ruin (0, 5) 도착 즉시 ruin_to_field 트리거 → field 복귀 → 다시 (14, 6) → 무한.

→ 도착지 슬롯 자동 평가를 **드롭**. 사용자가 도착 칸 떠난 후 다시 돌아와야 발화. 게임 디자인 결도 자연 (도착하자마자 돌아가는 결 없음).

## 검증

### 회귀 + 신규
| 테스트 | 결과 |
|---|---|
| test_event_1a / 1a2 / 1b / 2 | 102 / 102 |
| test_grid | 49 / 49 |
| test_save_load | 36 / 36 |
| test_multi_map_schema | 24 / 24 |
| **합** | **211 / 211 PASS** |

### 외부 동작 변경
신규 행동만 추가 (전이). 기존 행동 영향 0.

## 다음

사이클 3 — 신규 `test_multi_map_transition.gd` — 전이 / 복귀 / 회귀 시 시작 맵 자동 복귀 시뮬 검증.
