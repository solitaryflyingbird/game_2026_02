# M-3 사이클 1 검증 결과

**날짜:** 2026-05-03
**범위:** UI 다중 그리드 빌드 — 모든 맵 미리 빌드 + `current_map_id` 결로 가시성 토글

## 변경

### `run_ui.gd`
- 신설 `_grid_roots: Dictionary` (map_id → Control)
- 신설 `_tile_rects_by_map: Dictionary` (map_id → tile_rects)
- `_build_grid_display` 가 `MAPS` 의 모든 맵 그리드 미리 빌드 (시작 맵 + ruin_01)
- 신설 `_build_one_grid(map_id)` — 한 맵의 그리드 빌드
- 플레이어 마커 = 단일 인스턴스, 현 맵 root 결로 reparent
- `_refresh_grid_display` — `current_map_id` 결로 가시성 토글 + 플레이어 reparent
- `show_phase` / `_on_state_changed` — 그리드 root 결 갱신 (phase != "map" 이면 모든 root 숨김)

## 결정 사항

옵션 (a) lazy 다시 빌드 vs (b) 미리 빌드 + 토글 — **(b) 채택**. 맵 수 < 10 이면 메모리 결 무시 가능 + 단순. 전이 시 그리드 빌드 비용 0.

## 검증

### 회귀
| 테스트 | 결과 |
|---|---|
| test_event_1a / 1a2 / 1b / 2 | 102 / 102 |
| test_grid | 49 / 49 |
| test_save_load | 36 / 36 |
| test_multi_map_schema | 24 / 24 |
| test_multi_map_transition | 21 / 21 |
| **합** | **232 / 232 PASS** |

### 외부 동작 변경
시뮬 0. UI 결 — 그리드 root 가 맵 수 만큼 미리 만들어지고 가시성 토글. 시각 결로는 동일 (현 맵 그리드만 보임).

## 다음

사이클 2 — HUD 의 맵 이름 prefix (이미 사이클 5 에 박힘 — 검증 결로) + 전이 슬롯 마커 색 + 스크린샷 4 컷.
