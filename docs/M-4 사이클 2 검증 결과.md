# M-4 사이클 2 검증 결과 (M-4 종료 / 다중 맵 전체 종료)

**날짜:** 2026-05-03
**범위:** 옛 결 세이브 → 다중 맵 결 마이그레이션

## 변경

### `game_manager.gd`
- 신설 `_migrate_save(rd, brd)`:
  - `current_map_id` 누락 → `STARTING_MAP` 박힘
  - 옛 `visited_tiles` → `visited_by_map[STARTING_MAP]` 변환
  - 옛 `explored_tiles` → `explored_by_map[STARTING_MAP]` 변환
  - 옛 키 `erase`
- `load_save()` 가 로드 후 `_migrate_save` 호출

### `test/test_multi_map_save.gd`
- 옛 결 fixture 결로 마이그레이션 검증 추가 (7 항목)

## 검증

### 회귀 + 신규 (전체)
| 테스트 | 결과 |
|---|---|
| test_event_1a / 1a2 / 1b / 2 | 102 / 102 |
| test_grid | 49 / 49 |
| test_save_load | 36 / 36 |
| test_multi_map_schema | 24 / 24 |
| test_multi_map_transition | 21 / 21 |
| test_multi_map_visual | 12 / 12 |
| **test_multi_map_save (마이그레이션 추가)** | **19 / 19** |
| **합** | **263 / 263 PASS** |

### 외부 동작
세이브 / 로드 자체 동작 변경 0. 옛 결 세이브 자동 변환.

## M-4 종료

세이브 / 로드 다중 맵 호환 완료. 옛 결 세이브 (단일 맵 결로 박힌) 도 자동 변환.

## 다중 맵 (M-1 ~ M-4) 전체 종료

| M | 사이클 | 변경 핵심 |
|---|---|---|
| **M-1** | 5 (22 unit) | MAPS const + run_data 신규 필드 + UI 호환 + 옛 const/키 폐기 |
| **M-2** | 3 (10 unit) | ruin_01 정의 + transition 슬롯 양방향 + `_check_on_enter` 분기 |
| **M-3** | 2 (7 unit) | UI 다중 그리드 토글 + HUD 맵 이름 + 시각 검증 |
| **M-4** | 2 (6 unit) | 신규 결 직렬화 + 옛 세이브 마이그레이션 |

총 **12 사이클 / 45 unit**, 누적 **263 / 263 PASS**.

신규 검증 스크립트:
- `test_multi_map_schema.gd` (24)
- `test_multi_map_transition.gd` (21)
- `test_multi_map_visual.gd` (12)
- `test_multi_map_save.gd` (19)

## 다음

별도 차로 — 컨텐츠 / 에셋 / 리팩 / 그 외 작업.
