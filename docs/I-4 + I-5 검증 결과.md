# I-4 + I-5 검증 결과 — UI 인벤토리 + 세이브 마이그레이션

**날짜:** 2026-05-04
**범위:** 인벤토리 UI 패널 + HUD + 세이브 / 로드 / 마이그레이션 (RPG 결 종료)

## I-4 변경

### `run_ui.gd`
- 신규 변수: `_btn_show_inventory` / `_inventory_panel` / `_inventory_container` / `_inventory_visible` / `_inventory_hud_label`
- 신규 함수:
  - `_build_inventory_panel()` — 토글 버튼 + 패널 + 컨테이너
  - `_on_show_inventory_pressed()` — 토글
  - `_refresh_inventory_panel()` — **카테고리 자동 (CATEGORY_NAMES)** + **보유 > 0 만 표시** (빈 결 = "(인벤토리 비어있음)")
  - `_make_inventory_entry(entry)` — 한 결 (이름 / count / 사용 버튼)
  - `_format_inventory_hud(d)` — HUD 결의 보편 결 (보유 결만)
  - `_category_title(cat)` — `GameData.CATEGORY_NAMES.get` 결로 한국어 매핑
  - `_on_use_item_pressed(item_id)` — `RunManager.use_item` 호출
- `_on_state_changed` / `_refresh_grid_hud` 갱신 — 가시성 / HUD 결

## I-5 변경

### `game_manager.gd::_migrate_save`
- `big_run_data["inventory"]` 누락 → `{}` 박힘 (RPG 결의 빈 dict)
- `run_data["inventory"]` 누락 → big 의 사본
- `run_data["tools"]` 누락 → `{}` 빈 dict

## 검증

### 신규 — `test_inventory_visual.gd`: 10 / 10 PASS
- 빈 결 — HUD "(인벤토리 비어있음)" / 패널 빈 결 / 토글 (4)
- 일부 보유 — HUD 보편 결 (식량 5 / 응급수리키트 1 / 희귀 부품 3) / 패널 카테고리 (2)
- 사용 결 — repair_kit 사용 → erase + body_hp +25 + HUD 갱신 (4)
- 스크린샷 4 컷 (`inventory_01_empty` / `inventory_02_panel_empty` / `inventory_03_partial` / `inventory_04_after_use`)

### 신규 — `test_inventory_save.gd`: 9 / 9 PASS
- 신규 결 직렬화 + 복원 (5)
- 옛 결 마이그레이션 (4) — inventory / tools 결 없는 옛 세이브 → 자동 변환 (모두 빈 dict 박힘)

### 회귀 + 누적
| 결 | 결과 |
|---|---|
| 기존 9 + I-1, I-2, I-3 | 301 |
| **신규 inventory_visual** | **10 / 10** |
| **신규 inventory_save** | **9 / 9** |
| **누적** | **320 / 320 PASS** |

### 외부 동작
- UI 결 — phase=map 시 토글 버튼 + 패널 (보유 > 0 만 표시)
- HUD 결 — 보유 결만 (보편 결로 ITEMS 카탈로그 결과 자동 호환)
- 세이브 — 옛 세이브 자동 호환

## 인벤토리 도입 (I-1 ~ I-5) 종료

| 사이클 | 핵심 | 신규 검증 |
|---|---|---|
| **I-1** | ITEMS 카탈로그 + 빈 dict 결 자료구조 | 15 |
| **I-2** | helper (add / consume / use) — 0 도달 시 erase | 21 |
| **I-3** | effect type 4 + use_repair_kit / use_scanner | +14 (= 35 누적) |
| **I-4** | UI 인벤토리 (보유 > 0 만 / 카테고리 자동) | 10 |
| **I-5** | 세이브 마이그레이션 | 9 |

= **5 사이클 / 22 unit / 신규 69 검증 / 누적 320 PASS**.

## 다음

- 커밋 / 푸쉬?
- 또는 별도 차 (knowledge / 슬롯 의존 / 카운터 / 보상 풀 / 식량 정책 등)?
