# M-1 사이클 1 검증 결과

**날짜:** 2026-05-03
**범위:** `MAPS` const + `STARTING_MAP` + 옛 const legacy alias

## 변경

`game_data.gd`:
- 신설 `const MAPS` — `"field_01"` 1 개. `name` / `terrain` (16×12) / `spawn` (1, 6) / `encounters` (5 슬롯).
- 신설 `const STARTING_MAP = "field_01"`.
- `TERRAIN_RULES` / `ACTIONS_PER_DAY` / `DAY_MAX` 전역 그대로.
- 옛 const (`WORLD_TERRAIN` / `SPAWN_POS` / `TILE_ENCOUNTERS`) **legacy alias** 결로 임시 유지. 사이클 5 종료 시 폐기.

## 결정 사항

사이클 1 만으로 옛 const 폐기 시 참조처 (run_manager / run_ui / test) 5+ 곳 빌드 깨짐. 방법론 결 (사이클 종료 = 회귀 PASS) 보장 위해:
- **legacy alias 결**: 옛 const 를 데이터 사본 결로 유지
- 점진 마이그레이션 — 사이클 2~4 가 참조처를 새 결로 갱신
- 사이클 5 종료 시 alias 폐기 + grep 검증

## 검증

### 회귀
| 테스트 | 결과 |
|---|---|
| test_event_1a | 27 / 27 |
| test_event_1a2 | 28 / 28 |
| test_event_1b | 19 / 19 |
| test_event_2 | 28 / 28 |
| test_grid | 48 / 48 |
| test_save_load | 36 / 36 |
| **합** | **186 / 186 PASS** |

### 외부 동작 변경
0 — 옛 const alias 결로 모든 참조처 그대로 작동.

## 다음

사이클 2 — run_data 신규 필드 (`current_map_id` / `visited_by_map` / `explored_by_map`).
