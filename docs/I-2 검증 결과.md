# I-2 검증 결과 — helper 함수 (RPG 결)

**날짜:** 2026-05-04
**범위:** 인벤토리 helper 신설. count 0 도달 시 erase (RPG 결의 핵심).

## 변경

### `run_manager.gd` 신규 함수
- `_inventory_for_scope(scope)` — scope 결로 dict 분기
- `add_item(item_id, amount)` — 카탈로그 정의 검사 + stack_max cap
- `_consume_item(item_id, amount)` — 부족 시 false / **0 도달 시 key erase**
- `use_item(item_id)` — phase / 정의 / use_event_id null / 보유 0 거부 + 발화

## 검증

### 신규 — `test_inventory_actions.gd`: 21 / 21 PASS
- _inventory_for_scope 분기 (2)
- add_item — 정상 / stack_max cap / 카탈로그에 없는 id 거부 / 회차 한정 (8)
- **_consume_item — 정상 / 부족 거부 / 0 도달 시 erase** (4)
- use_item 거부 결 — phase / 정의 X / null / 보유 0 / phase != map (5)
- 정상 발화 결은 I-3 후 (use_repair_kit 박힌 후)

### 회귀
| 결 | 결과 |
|---|---|
| 기존 9 + I-1 | 266 |
| **신규 inventory_actions** | **21 / 21** |
| **누적** | **287 / 287 PASS** |

### 외부 동작 변경
0 — helper 신설만, 호출처 X.

## 다음

I-3 — 신규 effect type 4 (heal_body / give_item / remove_item / bump_initial_item) + EVENTS (use_repair_kit / use_scanner).
