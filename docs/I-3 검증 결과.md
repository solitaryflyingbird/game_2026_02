# I-3 검증 결과 — effect type + EVENTS

**날짜:** 2026-05-04
**범위:** 신규 effect type 4 + EVENTS (use_repair_kit / use_scanner). 인벤토리 결의 첫 작동.

## 변경

### `run_manager.gd`
- `apply_event_action` 의 match 에 4 분기 추가:
  - `heal_body` — body_hp += amount, max cap
  - `give_item` — `add_item` 결
  - `remove_item` — `_consume_item` 결
  - `bump_initial_item` — `big_run_data["inventory"][item] += amount` (RESEARCH 결)

### `game_data.gd::EVENTS`
- `use_repair_kit` — effect kind, `[{type: "heal_body", params: {amount: 25}}]`
- `use_scanner` — dialogue kind, 1 라인

## 검증

### 신규 — `test_inventory_actions.gd` 확장: 35 / 35 PASS
- I-2 의 21 + 신규 14:
  - heal_body 정상 + max cap (2)
  - give_item / remove_item (2)
  - bump_initial_item + 회귀 시 새 초기치 반영 (2)
  - use_repair_kit 사용 → repair_kit -1 (erase) + body_hp +25 (4)
  - use_scanner dialogue 발화 → advance → 종료 + scanner erase (4)

### 회귀
| 결 | 결과 |
|---|---|
| 기존 9 + I-1 + I-2 (원래) | 287 |
| **신규 inventory_actions (확장)** | **35 / 35** |
| **누적** | **301 / 301 PASS** |

### 외부 동작 변경
- 신규 effect type 4 — 기존 effect 영향 0
- 신규 EVENTS 2 — 사용처는 use_item 만

## 첫 작동 결

I-1 (그릇) + I-2 (도구) 박힌 후, **I-3 부터 진짜 작동**:
- 이벤트가 `give_item` / `remove_item` 결로 helper 호출
- use_item 결로 use_repair_kit / use_scanner 발화 + 인벤토리 가감

## 다음

I-4 — UI 인벤토리 패널 + HUD 표시.
