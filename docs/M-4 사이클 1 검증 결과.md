# M-4 사이클 1 검증 결과

**날짜:** 2026-05-03
**범위:** 다중 맵 결의 신규 세이브/로드 직렬화 검증

## 변경

### 신규
- `test/test_multi_map_save.gd` + `.tscn` (12 항목)
  - ruin_01 진입 후 세이브
  - 저장 파일 파싱 — `current_map_id == "ruin_01"`, `player_pos` Vector2i 결, `visited_by_map` 양 맵 결
  - load 후 모든 필드 round-trip 일치
  - load 후 try_move 정상

### 코드 변경 0
세이브 / 로드 결은 이미 `var_to_str` round-trip 으로 작동. 신규 필드 (`current_map_id` / `visited_by_map` / `explored_by_map`) 모두 자동 호환. 추가 코드 0.

## 검증

### 회귀 + 신규
| 테스트 | 결과 |
|---|---|
| test_event_1a / 1a2 / 1b / 2 | 102 / 102 |
| test_grid | 49 / 49 |
| test_save_load | 36 / 36 |
| test_multi_map_schema | 24 / 24 |
| test_multi_map_transition | 21 / 21 |
| test_multi_map_visual | 12 / 12 |
| **test_multi_map_save (신규)** | **12 / 12** |
| **합** | **256 / 256 PASS** |

### 외부 동작
세이브 / 로드 자체 변경 0. 신규 결 (current_map_id 등) 자동 직렬화.

## 다음

사이클 2 — 옛 결 (`visited_tiles` 키 박힌) 세이브 마이그레이션. `_migrate_save` 신설 + 옛 fixture 결로 검증.
